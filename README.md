# init/settings-website-builder

Website Builder bridge for global locale settings.

It exposes the shared `settings_locales` registry through `site.settings.locales.items` so the builder UI can manage locales without duplicating storage.

